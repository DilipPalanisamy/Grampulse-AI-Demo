# =============================================================================
# GramPulse AI - Database Environment Configuration
# =============================================================================

# PostgreSQL / PostGIS Database Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=grampulse_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSLMODE=prefer

# Alternative single connection URL (optional)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/grampulse_db
